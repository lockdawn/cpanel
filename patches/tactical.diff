diff --git a/static/tactical.css b/static/tactical.css
index 02ee5d879449363671db872fcc01a7471771559c..184564cc54e4c297cee9fc248efdef292ab35b9c 100644
--- a/static/tactical.css
+++ b/static/tactical.css
@@ -132,62 +132,66 @@ small {
 }
 
 .metric-list:last-child {
   margin-bottom: 0;
 }
 
 .metric-list.subtle {
   color: var(--muted);
   font-size: 0.9rem;
 }
 
 .metric-list.subtle li {
   color: inherit;
 }
 
 .bg-camo {
   background:
     radial-gradient(1200px 640px at -12% -18%, #182218 0%, rgba(24, 34, 24, 0) 58%),
     radial-gradient(1200px 640px at 108% 112%, #141d14 0%, rgba(20, 29, 20, 0) 60%),
     linear-gradient(180deg, #0B100B 0%, #0B0F0B 62%, #090D09 100%);
 }
 
 .app-shell {
   display: flex;
   min-height: 100vh;
+  width: 100%;
   background: transparent;
+  align-items: stretch;
 }
 
 .bg-panel {
   background: var(--panel);
 }
 
 .sidebar {
+  flex: 0 0 260px;
+  flex-basis: 260px;
   width: 260px;
   background: var(--panel);
   border-right: 1px solid var(--edge);
-  transition: width 0.25s ease;
+  transition: flex-basis 0.25s ease, width 0.25s ease;
   padding: calc(var(--space) * 1.5);
   gap: calc(var(--space) * 1.5);
   min-height: 100vh;
 }
 
 .sidebar .sidebar-brand {
   color: var(--text);
   font-size: 1.05rem;
   display: flex;
   align-items: center;
   gap: var(--space);
   padding-bottom: calc(var(--space) * 1.2);
   border-bottom: 1px solid rgba(40, 52, 40, 0.45);
 }
 
 .sidebar .brand-icon {
   width: 40px;
   height: 40px;
   display: grid;
   place-items: center;
   border-radius: 50%;
   background: rgba(168, 183, 137, 0.12);
   color: var(--accent);
   font-size: 1.2rem;
 }
diff --git a/static/tactical.css b/static/tactical.css
index 02ee5d879449363671db872fcc01a7471771559c..184564cc54e4c297cee9fc248efdef292ab35b9c 100644
--- a/static/tactical.css
+++ b/static/tactical.css
@@ -216,80 +220,83 @@ small {
 }
 
 .sidebar .nav-link .label {
   white-space: nowrap;
   transition: opacity 0.2s ease, max-width 0.2s ease;
 }
 
 .link-text {
   letter-spacing: 0.06em;
 }
 
 .sidebar .nav-link:hover,
 .sidebar .nav-link:focus-visible {
   background: rgba(168, 183, 137, 0.12);
   color: var(--text);
   outline: none;
 }
 
 .sidebar .nav-link.active {
   background: rgba(168, 183, 137, 0.18);
   color: var(--text);
   border: 1px solid rgba(168, 183, 137, 0.28);
 }
 
 .sidebar.collapsed {
+  flex: 0 0 78px;
+  flex-basis: 78px;
   width: 78px;
 }
 
 .sidebar.collapsed .sidebar-brand {
   justify-content: center;
 }
 
 .sidebar.collapsed .sidebar-brand .label {
   display: none;
 }
 
 .sidebar.collapsed .nav-link {
   justify-content: center;
   padding: 0.6rem 0;
 }
 
 .sidebar.collapsed .nav-link .label {
   opacity: 0;
   max-width: 0;
   margin: 0;
 }
 
 body.sidebar-collapsed .sidebar .nav-link {
   justify-content: center;
 }
 
 .main-panel {
   display: flex;
   flex-direction: column;
   flex: 1 1 auto;
+  min-width: 0;
   background: transparent;
   margin-left: 0;
 }
 
 .topbar {
   background: rgba(17, 22, 17, 0.96);
   border-bottom: 1px solid var(--edge);
   box-shadow: 0 6px 18px rgba(0, 0, 0, 0.35);
   position: sticky;
   top: 0;
   z-index: 1020;
 }
 
 .topbar-title {
   font-size: 0.95rem;
   letter-spacing: 0.24em;
 }
 
 .btn-sidebar {
   border-radius: 10px;
   border: 1px solid var(--edge);
   background: rgba(168, 183, 137, 0.12);
   color: var(--text);
   padding: 0.45rem 0.65rem;
   transition: background 0.2s ease, border-color 0.2s ease;
diff --git a/static/tactical.css b/static/tactical.css
index 02ee5d879449363671db872fcc01a7471771559c..184564cc54e4c297cee9fc248efdef292ab35b9c 100644
--- a/static/tactical.css
+++ b/static/tactical.css
@@ -318,50 +325,51 @@ body.sidebar-collapsed .sidebar .nav-link {
   background: rgba(168, 183, 137, 0.18);
   color: var(--text);
   border-color: var(--accent);
   outline: none;
 }
 
 .dropdown-menu-dark {
   background-color: rgba(18, 23, 18, 0.95);
   border: 1px solid var(--edge);
   border-radius: 10px;
 }
 
 .dropdown-menu-dark .dropdown-item {
   color: var(--muted);
   letter-spacing: 0.02em;
 }
 
 .dropdown-menu-dark .dropdown-item:hover,
 .dropdown-menu-dark .dropdown-item:focus-visible {
   background: rgba(168, 183, 137, 0.15);
   color: var(--text);
 }
 
 .main-content {
   flex: 1 1 auto;
+  min-width: 0;
   padding: calc(var(--space) * 2) calc(var(--space) * 2.5);
   display: flex;
   flex-direction: column;
   gap: calc(var(--space) * 2);
 }
 
 .page-heading {
   font-size: 1.25rem;
   letter-spacing: 0.18em;
 }
 
 .panel-card {
   background: var(--panel);
   border: 1px solid var(--edge);
   border-radius: var(--radius);
   box-shadow: var(--shadow);
   height: 100%;
   display: flex;
   flex-direction: column;
 }
 
 .panel-card-body {
   padding: calc(var(--space) * 1.8);
   display: flex;
   flex-direction: column;
